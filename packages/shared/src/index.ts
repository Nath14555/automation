/**
 * @louna/shared — Cross-app utilities.
 *
 * This package is intentionally empty for now. As the second agent comes
 * online, candidate code to extract here:
 *   - Anthropic SDK wrapper (function calling helpers, retry/back-off)
 *   - n8n REST/webhook utilities (HMAC verification, signed payloads)
 *   - Generic structured logger
 *   - Generic typed error hierarchy
 *   - Common Qdrant collection helpers
 *
 * App-specific code (env config, prompts, knowledge base, business logic)
 * stays inside the app's own `src/`.
 */

export {};
